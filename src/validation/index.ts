/**
 * Shared Zod schemas (cross-cutting shapes only).
 * Prefer co-locating feature schemas under each module as you implement them.
 *
 * Usage:
 *   router.post("/path", validate({ body: mySchema }), asyncHandler(handler));
 */

export {};
