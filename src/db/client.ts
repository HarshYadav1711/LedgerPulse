/**
 * Re-export generated Prisma client (see `prisma/schema.prisma` output path).
 * Keeps imports stable and avoids `@prisma/client` → node_modules resolution.
 */
export { PrismaClient, Prisma, Role, RecordType } from "../generated/prisma-client";
