/**
 * swagger-jsdoc requires at least one `apis` glob; the live spec is assembled from
 * `openapi.document.ts`. This file exists so the merge step runs without duplicating paths.
 *
 * @openapi
 * components: {}
 */
export {};
